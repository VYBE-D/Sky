import './globals.css'
export const metadata={title:'SKY — AI Investor Shadow Assistant',description:'Private investor intelligence and workflow assistant'}
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
