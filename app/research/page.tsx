'use client'
import { useState } from 'react'
export default function Research(){const [q,setQ]=useState('');const [busy,setBusy]=useState(false);const [result,setResult]=useState<any>(null);async function run(e:any){e.preventDefault();setBusy(true);const r=await fetch('/api/research',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({query:q})});const j=await r.json();setResult(j);setBusy(false)}return <div><div className="hero"><div><p className="eyebrow">RESEARCH CENTER</p><h1>Find the signal.</h1><p className="muted">Use OpenAI web search to investigate companies, founders, funding and market signals.</p></div></div><section className="panel"><form className="researchform" onSubmit={run}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Find AI startups raising under $3M…" required/><button className="button primary" disabled={busy}>{busy?'Researching…':'Run research'}</button></form><div className="chips"><button onClick={()=>setQ('Find AI startups raising under $3M')}>AI startups under $3M</button><button onClick={()=>setQ('Find founders looking for investors')}>Founders seeking capital</button><button onClick={()=>setQ("Find companies that recently announced funding")}>Recent funding</button></div></section>{result&&<section className="panel result"><p className="eyebrow">RESEARCH RESULT</p>{result.error?<div className="error">{result.error}</div>:<><h2>{result.title||'Web research completed'}</h2><pre>{result.output}</pre></>}</section>}</div>}
