'use client'
import { useState } from 'react'
import { createClient } from '@/lib/supabase/client'
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [signup,setSignup]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:any){e.preventDefault();setBusy(true);setError('');const s=createClient();const result=signup?await s.auth.signUp({email,password}):await s.auth.signInWithPassword({email,password});if(result.error)setError(result.error.message);else if(signup) setError('Check your email to confirm your account.');else location.href='/';setBusy(false)}return <main className="auth"><div className="authcard"><div className="brand big"><span className="brandmark">S</span> SKY</div><p className="eyebrow">PRIVATE INVESTOR INTELLIGENCE</p><h1>{signup?'Create your workspace':'Welcome back'}</h1><p className="muted">Research selectively. Surface opportunities. Keep the human in control.</p><form onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Password<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} /></label>{error&&<div className="error">{error}</div>}<button className="button primary full" disabled={busy}>{busy?'Working…':signup?'Create account':'Sign in'}</button></form><button className="textbutton" onClick={()=>setSignup(!signup)}>{signup?'Already have an account? Sign in':'New to Sky? Create an account'}</button></div></main>}
